#include<stdio.h> //include library file containing prebuilt functions & keywords

int main(void)  //writing vois is optional, even without it program will know by default that its void
{
  printf("Hello, world!");
  return 0; //tells program to return control to caller
}
